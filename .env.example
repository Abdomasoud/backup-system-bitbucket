# Bitbucket Backup System Configuration - Simple Version

# Bitbucket API Configuration (Updated for API Token)
ATLASSIAN_EMAIL=your-atlassian-email@domain.com
BITBUCKET_API_TOKEN=your-bitbucket-api-token
BITBUCKET_WORKSPACE=your-source-workspace
BACKUP_WORKSPACE=your-backup-workspace

# Backup Configuration
BACKUP_BASE_DIR=/opt/bitbucket-backup
MAX_BACKUPS=5
BACKUP_INTERVAL_DAYS=3

# Logging Configuration
LOG_LEVEL=INFO

# Performance Settings
PARALLEL_JOBS=2
COMPRESS_LEVEL=6
TEMP_CLEANUP=true

# How to get your API token:
# 1. Go to https://bitbucket.org/account/settings/app-passwords/
# 2. Click "Create app password"
# 3. Name: "Backup System"
# 4. Permissions: Repositories (Read, Write, Admin)
# 5. Copy the generated token and use it above

# Directory Structure will be:
# /opt/bitbucket-backup/
# ├── repositories/
# │   ├── repo1/
# │   │   ├── 20241002_120000.tar.gz
# │   │   ├── 20241005_120000.tar.gz
# │   │   └── ... (up to 5 backups)
# │   └── repo2/
# ├── metadata/
# └── logs/